PORT=5001
MONGO_URI=mongodb://localhost:27017/tadreeb
# Generate a long random string for production; never commit the real .env
JWT_SECRET=your_secret_key_here

# Optional: production frontend origin(s), comma-separated. If unset, CORS allows all origins (OK for local dev only).
# CORS_ORIGIN=https://your-frontend.com,https://www.your-frontend.com

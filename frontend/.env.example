# Copy to .env in this folder. CRA reads only vars prefixed with REACT_APP_.
# Local dev: defaults to http://localhost:5001/api if unset (see src/api/apiService.js)
# REACT_APP_API_URL=http://localhost:5001/api
